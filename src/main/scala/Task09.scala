/**
 * Input is a string (s) and a pattern (p). Pattern match the string.
 * . - any single character
 * * - 0 or more of the previous character
 * Matching must be done on the entire string.
 * String can either be empty or contain lowercase a-z letters only.
 * Pattern can either be empty or contain lowercase a-z letters and . * characters.
 */

object Task09 {
  def solution(s: String, p: String): Boolean = ???
}
