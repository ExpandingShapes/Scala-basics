/**
 * Input is a non-empty string and a non-empty word dictionary.
 * Add spaces into the string in order to create sentence where
 * all words are allowed by the dictionary. Return all such sentences.
 * Each word can be used several times.
 * All words in the dictionary are unique.
 */

object Task14 {
  def solution(
    s: String,
    wordDict: List[String]
  ): List[String] = ???
}
